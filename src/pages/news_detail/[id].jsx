import { Box, Typography } from "@mui/material"

const newsDetail = ({id}) => {
    return (
        <Box>
            <Typography>ini Product Detail</Typography>
        </Box>
    )
}

export default newsDetail